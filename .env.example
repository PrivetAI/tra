# Root .env example for VideoHarvester
# Copy to ".env" and adjust values as needed.

# =====================
# Backend/API settings
# =====================
# Port for the Express API
PORT=8080

# MongoDB connection string
# For local dev outside Docker, you can use: mongodb://127.0.0.1:27017/videoharvester
MONGO_URI=mongodb://mongo:27017/videoharvester

# Redis connection string
# For local dev outside Docker, you can use: redis://127.0.0.1:6379
REDIS_URL=redis://redis:6379

# Directory where downloaded files will be stored
# In Docker Compose this is mounted volume "/downloads"
DOWNLOADS_DIR=/downloads

# YouTube Data API v3 key (required for YouTube search/trends)
YOUTUBE_API_KEY=

# External scraper services (used by TikTok/Instagram workers)
TIKTOK_SCRAPER_URL=http://tiktok-scraper:8000
INSTAGRAM_SCRAPER_URL=http://instagram-scraper:8001

# Logging and polling
LOG_LEVEL=info
NODE_ENV=development
# Backend default poll interval for reference (frontend uses its own VITE_POLL_INTERVAL_MS)
POLL_INTERVAL_MS=2000

# =====================
# Frontend settings (Vite)
# =====================
# Base URL of the API. The frontend also auto-detects http://localhost:8080 when running locally.
VITE_API_URL=http://localhost:8080

# UI polling interval (ms) for task status
VITE_POLL_INTERVAL_MS=2000

